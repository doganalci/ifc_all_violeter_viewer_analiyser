"""ML alt-paketi: GAT modeli, eğitim döngüsü, statik analiz, görselleştirme.

Konsolide repo içinde `ml` bir Python paketidir. Tüm import'lar
`from ml.data.graph_loader import ...` formatında **mutlak** yapılır;
böylece kullanıcının `data/` (veri klasörü) Python'un namespace package
mekanizmasıyla `ml/data/`'yı gölgeleyemez.
"""
